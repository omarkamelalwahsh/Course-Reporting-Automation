# مستند شرح مشروع مصنف ومقترح الكورسات الذكي (AI Study Planner)

هذا المستند يقدم شرحاً مفصلاً للمشروع، يبدأ بالفكرة العامة وينتهي بتفصيل دور كل ملف برمجياً.

---

## 💡 فكرة المشروع (Project Concept)

المشروع هو نظام **مساعد ذكي** لمنصة "زدني" (Zedny) التعليمية. بدلاً من البحث التقليدي الذي يعتمد على تطابق الكلمات فقط، يستخدم هذا النظام **الذكاء الاصطناعي (Semantic Search)** ليفهم "المعنى" خلف بحث المستخدم.

**مثال:** إذا بحث المستخدم عن "كيفية إدارة الفرق"، سيجد النظام كورسات عن "القيادة" و"التواصل الإداري" حتى لو لم تحتوي كلمة "إدارة الفرق" صراحة، لأنه يفهم العلاقة الدلالية بينهما.

**أهداف المشروع:**

1. توفير واجهة بحث سلسة للمستخدمين (Streamlit).
2. توفير واجهة برمجية (API) للربط مع أنظمة أخرى مثل n8n.
3. تحليل الكتالوج التعليمي وتقديم تقارير ذكية عن التغطية والمدرسين.

---

## 📂 شرح ملفات المشروع (File-by-File Breakdown)

### الملفات في المجلد الرئيسي (Root)

- **`app.py`**: الملف المسؤول عن تشغيل واجهة المستخدم الرسومية (Streamlit). يحتوي على تصميم الصفحة، الفلاتر الجانبية، وعرض النتائج.
- **`api.py`**: واجهة برمجية (FastAPI) مبسطة وسريعة للربط مع الأدوات الخارجية.
- **`Dockerfile` & `docker-compose.yml`**: ملفات الحاويات لضمان تشغيل المشروع بنفس الإعدادات على أي جهاز.
- **`requirements.txt`**: قائمة المكتبات اللازمة لتشغيل واجهة الـ Streamlit والذكاء الاصطناعي.
- **`requirements.api.txt`**: المكتبات الإضافية لتشغيل الـ API المتقدم وتوليد ملفات PDF.
- **`smoke_test.py`**: سكريبت لاختبار النظام بسرعة والتأكد أن كل المكونات تعمل معاً بشكل صحيح.

### مجلد المصدر (`src/`)

هذا هو "مخ" المشروع، وينقسم إلى:

#### 1. منطق الذكاء الاصطناعي (`src/ai/`)

- **`embeddings.py`**: المسؤول عن تحويل النصوص (عنوان الكورس ووصفه) إلى أرقام (متجهات) يفهمها الحاسوب.
- **`gating.py`**: صمام الأمان؛ يتأكد أن بحث المستخدم منطقي وله علاقة بالكورسات المتاحة قبل استهلاك موارد البحث.
- **`ranker.py`**: يقوم بترتيب النتائج النهائية وإعطائها درجة من 10 تعبر عن مدى صلة الكورس ببحثك.

#### 2. الربط البرمجي (`src/api/`)

- **`main.py`**: نقطة الانطلاق لتطبيق FastAPI المتقدم.
- **`routes.py`**: يحدد "المسارات" أو الروابط التي يمكن للأنظمة الأخرى طلب البيانات منها (مثل رابط البحث، ورابط تحميل التقارير).
- **`filters.py`**: يحتوي على منطق الفلترة بناءً على الأدوار (مثلاً: إخفاء كورسات البرمجة عند البحث عن مهارات المحاسبة لفلان).
- **`schemas.py`**: يحدد شكل البيانات المدخلة والمخرجة لضمان دقتها.

#### 3. التقارير والذكاء (`src/reports/`)

- **`catalog_weekly.py`**: يقوم بتحليل كل الكورسات في المنصة لإنتاج إحصائيات عن أكثر التصنيفات نمواً وأكثر المدرسين نشاطاً.
- **`pdf_renderer.py`**: يأخذ البيانات ويحولها إلى صفحة HTML جميلة ثم إلى ملف PDF جاهز للطباعة.
- **`catalog_weekly_dashboard.py`**: مسؤول عن رسم الرسوم البيانية (Charts) داخل التقارير.

#### 4. ملفات النظام الأساسية

- **`pipeline.py`**: هو "المنظم" الذي يربط كل الخطوات ببعضها؛ من لحظة إدخال المستخدم للبحث وحتى خروج النتيجة النهائية.
- **`recommender.py`**: الفئة الرئيسية (Class) التي تتعامل مع فهرس البحث (FAISS) وتجلب الكورسات المتطابقة.
- **`config.py`**: يحتوي على كل الإعدادات مثل مسارات الملفات، ومفاتيح الربط (Tokens).
- **`data_loader.py`**: المسؤول عن قراءة ملفات الكورسات (CSV/Parquet) من الذاكرة.

---

## 🛠️ كيف يعمل النظام داخلياً؟ (Data Flow)

1. **الواجهة:** يدخل المستخدم جملة بحث في `app.py`.
2. **المعالجة:** يقوم `pipeline.py` بتنظيف النص والتأكد من لغته.
3. **البحث الهيكلي:** يقوم `recommender.py` بالبحث في ملف `faiss.index` (الذي يحتوي على معاني الكورسات المخزنة مسبقاً).
4. **التصفية:** يتم استبعاد الكورسات التي لا تناسب مستوى المستخدم أو الفلاتر المختارة عبر `filters.py`.
5. **العرض:** تظهر النتائج النهائية على الشاشة مع شرح "لماذا" تم اختيار هذا الكورس (`explain.py`).

---

## ⚠️ ملاحظات هامة

- **قواعد البيانات:** المشروع حالياً يعتمد على ملفات صلبة (`data/`) وليس قاعدة بيانات SQL لضمان السرعة في نماذج الـ MVP.
- **التخطيط:** ميزة "الخطط الدراسية المجدولة" (مثلاً: ماذا تدرس في الأسبوع الأول) غير موجودة حالياً في الكود، النظام يركز على "الاقتراح" والتحليل.

---

## 🚀 دليل البدء السريع

- **لتشغيل الواجهة:** `streamlit run app.py`
- **لتشغيل الـ API:** `uvicorn src.api.main:app --reload`
- **للتعديل على منطق البحث:** توجه إلى `src/pipeline.py`.
